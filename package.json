{
  "private": true,
  "scripts": {
    "ci:version:prerelease": "pnpm changeset version",
    "ci:version:release": "pnpm changeset pre exit && pnpm changeset version && pnpm changeset pre enter pre",
    "ci:publish": "pnpm changeset publish"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
